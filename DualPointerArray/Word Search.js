const test1 = [
    ['A','B','C','E'],
    ['S','F','C','S'],
    ['A','D','E','E']
];

var exist = (board, word) => {
    let letter = args[2]

    // if we get to the end of the array return false 
    if (the thing has reached the end or found the word) {
        return 
    }

    exist(board, word, currentLocation)
};

console.log(exist(test1, "SEE"), True)
console.log(exist(test1, "POOP"), False)