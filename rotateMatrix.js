/*
    forEach column reverse the order 
    STORE temp values for each of the columns and then add to the end 
    Remove the original ones from the beginning Slice(0, length of original array)
    forEach   
*/

var rotate = function(matrix) {
    const originalLength = matrix.length


    // matrix.forEach(array, i => {
    //     let matrixlevel = [];

    //     matrixlevel.push(array[i])
    // });
};
